<div class="row">
  <div class="col-md-6 layout">
    <h3><%= @title if @stock_summary != "" %> A</h3>
    <p>The people disagree on these points:</p>
    <% summary.counter_points.each do |point, counter| %>
      <% #point = Condense.condense_group([point["String"], counter["String"]]) %>
      <% point = [point["String"], counter["String"]] %>
      <% if point.size == 2 %>
        <blockquote>
          <p>
            <%= Presenter.format(point.first, summary.topics) %>
          </p>
        </blockquote>
      <% else %>
        <blockquote><p><%= Presenter.format(point.first, summary.topics) %></p></blockquote>
      <% end %>
    <% end %>

    <!-- <p>The following negated points were discussed:</p> -->
    <% summary.negated_points.each do |_, first, _| %>
      <blockquote>
        <p><%= Presenter.format(first["String"], summary.topics) %></p>
      </blockquote>
    <% end %>

    <p>These are examples of common pairs of points raised by the same user:</p>
    <% summary.related_points.each do |point, related| %>
      <blockquote>
        <p>
          <%= Presenter.format(point["String"], summary.topics) %>
           &amp;
          <%= Presenter.format(related["String"], summary.topics) %>
        </p>
      </blockquote>
    <% end %>

    <p>Other commonly occurring points made in the discussion were:</p>
    <% summary.common_points.each do |point| %>
      <blockquote>
        <p><%= Presenter.format(point["String"], summary.topics) %></p>
      </blockquote>
    <% end %>

    <p>Common points made in the discussion that link concepts were:</p>
    <% summary.longer_points.each do |point| %>
      <blockquote>
        <p><%= Presenter.format(point["String"], summary.topics) %></p>
      </blockquote>
    <% end %>

    <p>Points for commonly discussed topics:</p>
    <ul>
      <% summary.commonly_discussed_topic_points.each do |topic, points| %>
        <li>
          <p><%= topic.capitalize %>
          <% #Condense.condense_group(points.map { |p| Presenter.clean(p["String"]) }).each do |point| %>
          <% points.map { |p| Presenter.clean(p["String"]) }.take(@summary.point_count).each do |point| %>
            <blockquote>
              <p><%= Presenter.format(point, summary.topics) %></p>
            </blockquote>
          <% end%>
        </li>
      <% end %>
    </ul>

    <p>Points about multiple topics:</p>
    <% summary.multiple_topic_points.each do |point| %>
      <blockquote>
        <p><%= Presenter.format(point["String"], summary.topics) %></p>
      </blockquote>
    <% end %>

    <p>People ask questions like:</p>
    <% summary.question_points.each do |point| %>
      <blockquote>
        <p><%= Presenter.format(point["String"], summary.topics, true) %></p>
      </blockquote>
    <% end %>

    <% if false %>
      <hr/>
      <% meta = summary.metadata %>
      <p>
        Summary based on
        <%= meta[:points] %> points from
        <%= meta[:posts] %> posts. There were
        <%= meta[:groups]%> groups of equivalent points.
      </p>
    <% end %>
  </div>

  <div class="col-md-6 plain">
    <h3><%= @title if @stock_summary != "" %> B</h3>
    <% @stock_summary.split("\n").each do |line| %>
      <p><%= line %></p>
    <% end %>
  </div>
</div>
